/**
 * Session configuration constants.
 * Change IDLE_TIMEOUT_MS to adjust how long a logged-in user can be idle
 * before being automatically signed out.
 */

/** Inactivity timeout in milliseconds (default: 2 hours) */
export const IDLE_TIMEOUT_MS = 2 * 60 * 60 * 1000; // 2 hours
